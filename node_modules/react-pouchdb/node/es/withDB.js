import _extends from "@babel/runtime/helpers/extends";
import _Object$assign from "@babel/runtime/core-js/object/assign";
import React from "react";
import { forwardRef } from 'react';
import PouchDBModule from "pouchdb-node";
import { instanceOf } from 'prop-types';
import hoistStatics from 'hoist-non-react-statics';
import { Consumer } from './DBContext';
export var propTypes = {
  db: instanceOf(PouchDBModule).isRequired
};

var withDB = function withDB(Component) {
  return forwardRef(hoistStatics(_Object$assign(function (props, ref) {
    return React.createElement(Consumer, null, function (db) {
      return React.createElement(Component, _extends({}, props, {
        db: db,
        ref: ref
      }));
    });
  }, {
    displayName: "withDB(".concat(Component.displayName || Component.name, ")"),
    WrappedComponent: Component
  }), Component));
};

export default withDB;
//# sourceMappingURL=withDB.js.map